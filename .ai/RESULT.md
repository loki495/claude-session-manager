# RESULT.md — OpenCode TUI agent integration

## Discoveries

- 2026-08-24: OpenCode v1.18.21 installed at `/usr/bin/opencode` on this machine, already authenticated and in use (14 sessions in `~/.local/share/opencode/opencode.db`, including this orchestration session itself `ses_fc894e9f0ffeltA15imtgaZocS`).
- 2026-08-24: OpenCode's storage is SQLite (`opencode.db`), not JSONL — tables `session`/`message`/`part`/`todo`/`session_input`/`permission`/`project`/`event`. Transcript reads will need a new `OpenCodeTranscriptService` that queries `message`+`part` by `session_id`, not a JSONL tail.
- 2026-08-24: OpenCode TUI positional arg is the project directory (`opencode [project]`), flags `--model`, `--agent`, `--session`, `--continue`, `--auto` mirror Claude/Antigravity's per-session options structurally, but `--session` is resume-only (no `--session-id` for new sessions observed in `--help`).
- 2026-08-25: Plugin system review (amendment a) — `https://opencode.ai/docs/plugins/` + local `plugin/dist/index.d.ts` + `sdk/dist/gen/types.gen.d.ts`: 13+ event types including `permission.updated`/`permission.replied`, `session.status` (idle/busy/retry), `session.idle`, `message.updated`; Hooks `permission.ask` (ask/deny/allow decision), `tool.execute.before/after`, `event`. SDK `tui.d.ts` exposes `permission(sessionID)` and `TuiAttentionSoundNames` with permission/question/error/done variants. Load order global→project, local file or npm. This is the intended replacement for `capture-pane` parsing — Q1.2 updated with preliminary assessment, 0.2 will live-confirm.
- 2026-08-25: Task 0.2 live verification (isolated tmux `/tmp/csm-research-*/socket`, real `opencode` binary, 200×50 panes):
  - Session ID is reactive: `opencode --session <nonexistent>` → `Error: Session not found` (resume-only); plain `opencode /tmp` spawns but creates no DB row until first prompt. `opencode run --format json` streams `sessionID` in first `step_start` event and creates DB row (`ses_fc84b56dd...`, `directory=/tmp`). Sidecar must start with `claude_session_id=NULL` like Antigravity.
  - TUI capture-pane is blank (default + `-a` alt buffer) even though `opencode` process runs at 148% CPU — `fish -c opencode` wrapper visible, but nothing capturable. `send-keys "opencode --help"` DOES render, so tmux plumbing is fine; TUI's own rendering likely uses alternate-screen/raw-mode. Implication: blocked-prompt detection must be plugin-fed (`permission.ask` / `event: permission.updated`), not pane-scraped.
  - Concurrent SQLite reads safe: `PRAGMA journal_mode=wal`, `SELECT COUNT(*) FROM session` + `SELECT data FROM message WHERE session_id=?` succeed while TUI runs (no lock).
  - Plugin dirs `~/.config/opencode/plugins/` and `.opencode/plugins/` do not exist yet — no live plugin test in 0.2; Phase 5 will create `csm-status.ts` and live-verify.

## Decisions

- 2026-08-25: Config groundwork uses `OPENCODE_BIN` env + `Config::opencode_bin()` mirroring `claude_bin()`/`antigravity_bin()` exactly; no generalized `Config::agent_bin()` dispatcher (same deviation as Antigravity Phase 0).
- 2026-08-25: `sidecars.agent TEXT` already handles arbitrary values including 'opencode' — no migration needed (verified via SqliteDb::sessions_schema()).

## Worker iterations

- 2026-08-25: Task 0.1 — worker `ses_fc85067fbffe0xVvxiZrZEkVtz` (muse-spark-1.2-contributor-free) added `Config::opencode_bin()` + `host-agent/.env.example` `OPENCODE_BIN` entry. `php -l` OK, `bash tests/run.sh` PASS (all tests passed). Orchestrator verified and marked done.
- 2026-08-25: Task 0.2 — orchestrator-direct (isolated tmux + real `opencode` binary, WAL + reactive-ID verified). Spawned `opencode /tmp` in throwaway tmux (200×50), confirmed reactive ID, blank capture-pane, WAL safety. Updated QUESTIONS.md Q1 + RESULT.md.
- 2026-08-25: Task 1.1 — orchestrator-direct (worker `ses_fc84a155cffevMcfBn1JKtfgZl` stalled on permission loop). Created `host-agent/lib/Agents/OpenCodeAdapter.php` (id=opencode, prefix=oc, `build_spawn_argv` with positional workdir + --model/--agent, assigned_id=null), registered in `AgentRegistry`, mirrored `PageView::AGENT_OPTIONS`. Extended `tests/test_agent_adapter.php` (18 new assertions: identity, workdir/model/agent, empty-model, ignored keys, permission_mode_map=[], honest check_hooks stub). `php -l` OK, `bash tests/run.sh` PASS (all tests passed).
- 2026-08-25: Task 2.1 — orchestrator-direct. Verified `create_cc_session()` is already generic (no change to `Sessions.php`/`SessionLifecycleService.php`/`DashboardController.php` — whitelist + adapter dispatch + sidecar `agent` already handle opencode). Created `tests/fixtures/fake_opencode` (blocks like `cat` via `exec -a`, distinct from `fake_claude`/`fake_agy`), added `OPENCODE_BIN` to `tests/.env.testing`, created `tests/test_opencode_spawn.php` (16 assertions: oc-* prefix, sidecar `agent=opencode`, `claude_session_id=null` for reactive binding, list `agent/agent_label`, pane `fake_opencode`, fallback to `cc-` for unknown/null agent, kill + sidecar cleanup, rejects unknown). `bash tests/run.sh` PASS (all 21 test files).
- 2026-08-25: Task 3.1 — orchestrator-direct (worker `ses_fc828fad8ffezRw54k63VUMaUw` stalled on `read {}` with empty filePath — big-pickle tool-generation loop). Implemented `host-agent/lib/Services/OpenCodeTranscriptService.php` (reads `opencode.db` SQLite via `Config::opencode_db_path()`, WAL+readonly+busy_timeout, maps message role + text/tool/file/reasoning/step-start parts to canonical `{type,role,timestamp,blocks:[{kind,text}]}` with hard-cap truncation, tool parts produce tool_use+tool_result blocks), added `Config::opencode_db_path()`, wired `TranscriptRouter` (`is_opencode_path` via `ses_*` shape, routes all three methods). Live-verified against real `ses_fc894e9f0ffeltA15imtgaZocS` (tail pagination, user text + 6-block tool interleaving). Created `tests/test_opencode_transcript.php` (62 assertions: find/is_opencode, router integration, read pages with 4 entries + 2 filtered skipped, pagination has_more/next_before, untilRealUserMessage, page_since forward poll, missing/empty IDs, router dispatch, attachment stub, long-text truncation, missing DB). `bash tests/run.sh` PASS (all 22 test files).
- 2026-08-25: Task 4.1 — orchestrator-direct. Verified `build_session_entry()` already generically handles `agent=opencode` (list `agent/agent_label` proven in test_opencode_spawn). Added `OpenCodeTranscriptService::find_session_title()` (reads `session.title` from `opencode.db`) and wired `SessionService::session_title()` (`is_opencode_path` guard + title from DB vs `find_latest_ai_title` for Claude). Verified: null/bad id falls through to workdir, real opencode session `ses_fc84b56ddffeqmBhbrqlDo6K56` returns title "Hello world in exactly 3 words" via DB. `bash tests/run.sh` PASS (all 22).
- 2026-08-25: Reactive binding fix (blocking bug for live oc sessions): `oc-20260825-075821` started via UI but transcript stayed empty — sidecar `claude_session_id=null` until first prompt creates `ses_*` (reactive like Antigravity, see Q1.1). Fixed by `OpenCodeTranscriptService::find_session_for_workdir(workdir, spawnedAt)` (most recent session with matching directory, dedup against already-bound ids, WAL+readonly) + self-heal in `SessionService::build_session_entry()` and `SessionDetailService::session_history()/session_attachment()` (polls opencode.db, writes to sidecars when match found). Live-verified: `oc-20260825-075821` healed to `ses_fc8124fe3ffeeA42w2CPV5bW0f` on next `list_all_sessions` poll, `session_history` returned 4 entries (user "Can you check this project..." + assistant tool reads), `bash tests/run.sh` PASS.
- 2026-08-25: Task 6 — backend (quotas, amendment b) — orchestrator-direct. Added `QuotaService::opencode_quota_state(?sessionId)` (direct WAL+readonly read from `opencode.db`, per-session cost/tokens_* or dashboard aggregate cost+tokens+session_count, no GlobalStateStore/timer), wired `get_quota(sessionName)` for opencode agent and `get_quota()` dashboard `agents.opencode` map. Live-verified: per-session `ses_fc8124` → cost 0, tokens 16k/907, dashboard aggregate 24 sessions, 3.1M input. Updated `tests/test_quota.php` + `test_sessions_lifecycle.php` to isolate `OPENCODE_DB_PATH` via temp fixture for "no data" cases. `bash tests/run.sh` PASS (all 22).

## Verification

- 2026-08-25: `bash tests/run.sh` PASS after 0.1 (full suite, no regressions).
