# Orchestrator/Worker Protocol — moved

This protocol has been revised, made tool-agnostic (Claude Code / opencode / Codex /
agy), and promoted to a shared skill so every agent on this machine has the same
understanding of the orchestrator/worker split, not just opencode:

    ~/dotfiles/ai/skills/orchestrator-worker/SKILL.md

That's the canonical, maintained version. This file is kept only as a pointer for
anyone following an old reference to this path.
